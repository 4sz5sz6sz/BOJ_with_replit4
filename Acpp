#include<bits/stdc++.h>
using namespace std;

int main(){
	ios::sync_with_stdio(0);
	cin.tie(0); cout.tie(0);

	int a,b,c;
	int ans;
	cin >> a >> b >> c;
	if(a+c > b)
		ans = a+c;
	else
		ans = b;
	cout << ans << "\n";
	
	return 0;
}